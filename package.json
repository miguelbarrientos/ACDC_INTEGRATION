{
  "name": "acdc-server-demo",
  "version": "1.0.0",
  "description": "ACDC PayPal demo — server-side (Express + native fetch, Node ≥18)",
  "main": "server.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.18.2"
  }
}
